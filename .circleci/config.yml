version: 2
jobs:
  build:
    machine: true
    steps:
      # ...
      - run:
          name: Build image
          command: |
            pwd
      - run:
          name: Build image
          command: docker build -f ~/project/Dockerfile
      - run:
          name: Test image
          command: |
            docker run -d -p 8080:8080 --name built-image test
